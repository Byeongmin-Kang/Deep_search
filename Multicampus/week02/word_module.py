# -*- coding: utf-8 -*-
"""
Created on Wed Jul  5 13:47:37 2017

@author: user
"""

#word_module
#단어 출력함수
"""
def outword():
    for item in 
"""
"""
# list에서 단어 하나를 추출하는 함수
def viewword(index):
    for i in range(len(wordlist)):
        print()
        return wordlist[i]

"""
# 제외 함수
def shortchangeword(word,e_word):
    #입력받은 단어를 list로 변경한다.
    word_l = list(word)
    for item in range(len(word_l)):
        if e_word == word_l[item]:
            word_l[item] = '_'
    word_p = "".join(word_l)
    return word_p

def longchangeword(word,e_word1,e_word2):
    word_l = list(word)
    for item in range(len(word_l)):
        if e_word1 == word_l[item] or e_word2 == word_l[item]:
            word_l[item] = '_'
    word_p = "".join(word_l)
    return word_p

def rechangeword(word,e_word):
    
    return
"""
for i in range(len(wordlist))
if len(wordlist)
"""
"""
wordlist = []

for i in range(10):
    name_list = input("입력하세요(%d번째)>>"%(i+1))
    insert(name_list)
    
while True:
    i = 0
    word = changeword(wordlist(i),'a')
    print(word)
    i = i + 1
"""
"""
e_word
in_word = input('알페벗을입력해주세요 ')
def compare_word(e_word,in_word):
    if e_word==in_word:
"""